'''
FUNCTION my_traceroute():
    1. parse_args()  # parse command-line args: -n, -q, -S, and the destination

    2. resolve destination_host to IP_address (if not numeric_only, you might want to do reverse-DNS later for intermediate hops)

    3. create sockets:


    4. set a maximum number of hops (e.g., 30) or let the user specify

    5. hop = 1
    6. WHILE hop <= max_hops:
        
    7. close sockets
END FUNCTION

'''